// client/src/pages/About/HeroSection.tsx
import styles from './HeroSection.module.css';

const HeroSection = () => {
    return (
        <header className={styles.header}>
            <h1 className={styles.title}>
                ABOUT
            </h1>
        </header>
    );
};

export default HeroSection;